# Third-party notices

This project uses open-source packages distributed through npm. Their license
texts and metadata are available in the installed package directories and the
upstream repositories referenced by `package-lock.json`.

The project does not bundle third-party training videos, photographs, music, or
pose overlays. Any future example media must document its source and license in
the example folder before publication.
